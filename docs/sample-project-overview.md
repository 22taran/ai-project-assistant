# Sample Project Overview

The KT Assistant answers questions about this project's documentation. This seed
file exists so the Knowledge Base has retrievable content on first sync.

## Deployment
Run `terraform apply` in `envs/dev`, then start a Bedrock ingestion job to index
these docs. The Slack bot answers questions grounded in this content.
